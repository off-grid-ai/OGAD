/**
 * TEMPORARY re-export shim (hexagonal program 2, seat C). Owner: `@offgrid/automation`
 * (`task-step-detail`). Delete when every importer listed under this shim in
 * shared/docs/hexagonal-program-2/PROGRESS_C.md imports `@offgrid/automation` directly; Agent A
 * flips the renderer and pro importers in the same cutover.
 */
export type { ComputerUsePhase, ComputerUseStepDetail } from '@offgrid/automation'
