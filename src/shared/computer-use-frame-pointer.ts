/**
 * TEMPORARY re-export shim (hexagonal program 2, seat C). Owner: `@offgrid/automation`
 * (`web-use-coordinates`). Delete when `pro/renderer/components/browser/tasks/ComputerUseLiveView.tsx`
 * and `pro/main/sync/{task-visual-step-projection,task-run-projection}.ts` import
 * `@offgrid/automation` directly.
 */
export {
  computerUseFramePointer,
  type ComputerUseFramePointer,
  type ComputerUseFrameEvidence
} from '@offgrid/automation'
